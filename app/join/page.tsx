'use client';
import { useState } from 'react';
export default function Join(){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();setMsg('Saving...');const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch('/api/tryouts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setMsg(r.ok?'Tryout request saved.':'Save failed.');if(r.ok)e.currentTarget.reset()}const fields=['name','game_name','instagram_id','active_time','driving_skill','game_skill'];return <main className="section"><div className="wrap"><div className="center"><b className="red">JOIN</b><h1 className="h2">Tryout Form</h1><p className="muted">Submit request to join SOUL.</p></div><form onSubmit={submit} className="card form" style={{maxWidth:720,margin:'auto'}}>{fields.map(f=><input key={f} className="input" name={f} placeholder={f.replaceAll('_',' ')} required/>)}<textarea className="input" name="message" placeholder="Message" rows={4}/><button className="btn red">Submit Tryout</button>{msg&&<p className="toast">{msg}</p>}</form></div></main>}