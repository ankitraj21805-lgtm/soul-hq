'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';

const seedMembers:any[] = [
 ['1','Ankit Sharma','SOUL Ankit','Founder','Core','Active',92,88,0,'Promoted'],['2','Rahul Verma','SOUL Phantom','Co-Leader','Core','Active',87,84,1,'Eligible'],['3','Aman Singh','SOUL Viper','Captain','Alpha','Active',90,86,0,'Promoted'],['4','Rohit Kumar','SOUL Nitro','Elite Driver','Alpha','Active',94,80,0,'Eligible'],['5','Aditya Raj','SOUL Shadow','Shooter','Alpha','Active',81,91,1,'Hold'],['6','Kunal Das','SOUL Blaze','Member','Bravo','Trial',77,78,0,'Trial Running'],['7','Deepak Yadav','SOUL Racer','Driver','Bravo','Active',89,75,0,'Eligible'],['8','Sahil Khan','SOUL Reaper','Member','Bravo','Active',78,83,2,'Role Hold'],['9','Nikhil Jain','SOUL Hawk','Scout','Charlie','Active',82,82,0,'Promoted'],['10','Mohit Patel','SOUL Mafia','Member','Charlie','Hold',75,76,2,'Hold'],['11','Harsh Gupta','SOUL King','Content Lead','Media','Active',80,79,0,'Eligible'],['12','Vikas Thakur','SOUL Crown','Moderator','Core','Active',84,81,0,'Promoted'],['13','Aryan Mehta','SOUL Drift','Driver','Alpha','Active',93,77,0,'Eligible'],['14','Piyush Rai','SOUL Ghost','Member','Charlie','Trial',73,76,0,'Trial Running'],['15','Satyam Roy','SOUL Bullet','Shooter','Bravo','Active',79,90,1,'Eligible'],['16','Yash Sharma','SOUL Knight','Member','Charlie','Inactive',70,70,3,'Remove Review'],['17','Manish Kumar','SOUL Falcon','Event Lead','Core','Active',83,82,0,'Promoted'],['18','Saurav Mishra','SOUL Venom','Member','Bravo','Active',76,84,1,'Hold'],['19','Abhishek Tiwari','SOUL Royal','Elite','Alpha','Active',88,87,0,'Eligible'],['20','Ritesh Pandey','SOUL Wolf','Member','Charlie','Trial',74,75,0,'Trial Running']
].map(m=>({id:m[0],real_name:m[1],game_name:m[2],role:m[3],team:m[4],activity_status:m[5],driving_skill:m[6],game_skill:m[7],warnings_count:m[8],promotion_status:m[9],alliance_tag:'SYN'}));
const seedEvents = [{id:'e1',title:'SOUL Night Run',type:'Clan Meet',time:'10:00 PM',status:'Planned'},{id:'e2',title:'SYN Alliance Meetup',type:'Alliance',time:'9:30 PM',status:'Planned'},{id:'e3',title:'Content Shoot',type:'Media',time:'11:00 PM',status:'Draft'}];
const seedAllies = [{id:'a1',clan_name:'Royal Drift Crew',alliance_tag:'SYN',leader_instagram:'@royaldrift',status:'Active'},{id:'a2',clan_name:'Night Racers',alliance_tag:'SYN',leader_instagram:'@nightracers',status:'Pending'},{id:'a3',clan_name:'Blackline Mafia',alliance_tag:'SYN',leader_instagram:'@blackline',status:'Active'}];
const nav = ['Home','About','Members','Join','Events','Alliance','Gallery','Rules','Login','Admin'];
function path(n:string){ return n==='Home' ? '/' : '/' + n.toLowerCase(); }
function Shell({children}:any){ return <><header className="nav"><div className="wrap navin"><Link className="brand" href="/">👑 SOUL HQ</Link><nav className="links">{nav.map(n=><Link key={n} href={path(n)}>{n}</Link>)}</nav></div></header>{children}<footer className="section"><div className="wrap muted">SOUL Syndicate • SYN Alliance • Respect • Loyalty • Skill • Fair-play only</div></footer></>; }
function Title({e,t,d}:any){ return <div className="center"><b className="red">{e}</b><h1 className="h2">{t}</h1><p className="muted">{d}</p></div>; }
function Home(){ const modules=['Member CRUD','Daily Attendance','Warning Tracker','Promotion Status','Tryout Pipeline','Event Manager','Alliance Manager','Content Planner','Settings']; return <main><section className="hero"><div className="wrap grid grid2"><div><span className="tag">SOUL Syndicate • MadOut 2 HQ</span><h1 className="title">Premium clan control for <span className="red">SOUL</span>.</h1><p className="muted">Dark mafia-inspired full-stack clan website with public pages and a working admin dashboard.</p><div className="row" style={{marginTop:24}}><Link className="btn red" href="/join">Join Tryout</Link><Link className="btn" href="/members">Members</Link><Link className="btn" href="/admin">Admin</Link></div></div><div className="card"><p className="muted">Official alliance tag</p><h2 className="title red">SYN</h2><p>Any player using SYN tag should not be attacked by allied clans.</p><div className="grid grid2" style={{marginTop:18}}><div className="card"><div className="stat">20</div><p className="muted">Seed Members</p></div><div className="card"><div className="stat">10</div><p className="muted">IN Points</p></div></div></div></div></section><section className="section"><div className="wrap grid grid3">{modules.map(m=><div className="card" key={m}><h3 className="red">{m}</h3><p className="muted">Live management module for SOUL HQ.</p></div>)}</div></section></main>; }
function About(){ return <main className="section"><div className="wrap"><Title e="ABOUT" t="SOUL Syndicate" d="A disciplined MadOut 2 clan built around respect, loyalty and skill."/><div className="grid grid3"><div className="card"><h3>Identity</h3><p className="muted">Premium SOUL HQ clan control room.</p></div><div className="card"><h3>Culture</h3><p className="muted">Fair-play, clean rules and strong roles.</p></div><div className="card"><h3>Alliance</h3><p className="muted">Official protection tag is SYN.</p></div></div></div></main>; }
function Members(){ return <main className="section"><div className="wrap"><Title e="ROSTER" t="SOUL Members" d="Twenty sample members for your clan HQ."/><div className="grid grid3">{seedMembers.map(m=><div className="card" key={m.id}><div className="row" style={{justifyContent:'space-between'}}><h3>{m.game_name}</h3><span className="tag">SYN</span></div><p className="muted">{m.real_name} • {m.role}</p><p>Team: {m.team} • Status: {m.activity_status}</p><p>Driving {m.driving_skill} • Game {m.game_skill}</p><p>Warnings: {m.warnings_count} • {m.promotion_status}</p></div>)}</div></div></main>; }
function Join(){ const [msg,setMsg]=useState(''); async function submit(e:any){ e.preventDefault(); setMsg('Saving...'); const body=Object.fromEntries(new FormData(e.currentTarget).entries()); const res=await fetch('/api/tryouts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}); setMsg(res.ok?'Tryout request saved. Admin dashboard me show hoga.':'Save failed. Supabase keys/schema check karo.'); if(res.ok) e.currentTarget.reset(); } const fields=['name','game_name','instagram_id','active_time','driving_skill','game_skill']; return <main className="section"><div className="wrap"><Title e="JOIN" t="Tryout Form" d="Apply for SOUL Syndicate."/><form onSubmit={submit} className="card form" style={{maxWidth:720,margin:'auto'}}>{fields.map(f=><div key={f}><label>{f.replaceAll('_',' ')}</label><input className="input" name={f} required /></div>)}<div><label>message</label><textarea className="input" name="message" rows={4}/></div><button className="btn red">Submit Tryout</button>{msg&&<p className="toast">{msg}</p>}</form></div></main>; }
function Events(){ return <main className="section"><div className="wrap"><Title e="EVENTS" t="Clan Schedule" d="Plan meets, alliance sessions and content posts."/><div className="grid grid3">{seedEvents.map(e=><div className="card" key={e.id}><h3>{e.title}</h3><p>{e.type} • {e.time}</p><span className="tag">{e.status}</span></div>)}</div></div></main>; }
function Alliance(){ return <main className="section"><div className="wrap"><Title e="ALLIANCE" t="Official tag: SYN" d="Any player using SYN tag should not be attacked by allied clans."/><div className="grid grid3">{seedAllies.map(a=><div className="card" key={a.id}><h3>{a.clan_name}</h3><p>Tag: <b className="red">{a.alliance_tag}</b></p><p className="muted">Leader: {a.leader_instagram} • {a.status}</p></div>)}</div></div></main>; }
function Gallery(){ const items=['Sports cars','Red neon city','Crown squad','SYN meet','Clan win','Role reveal']; return <main className="section"><div className="wrap"><Title e="GALLERY" t="SOUL Media" d="Premium frames for clan visuals and Instagram content."/><div className="grid grid3">{items.map(x=><div className="card" key={x}><h3 className="red">{x}</h3><p className="muted">Add your clan image here later.</p></div>)}</div></div></main>; }
function Rules(){ const rules=['Respect every member','No hacks, cheats or exploits','IN played gives 10 points','Late gives 7 points','OUT with reason gives 5 points','No reply gives 0 points','Warning 1 is reminder','Warning 2 is role hold','Warning 3 is remove review']; return <main className="section"><div className="wrap"><Title e="RULES" t="Clan Standards" d="Simple rules for clean and fair clan management."/><div className="grid grid3">{rules.map(r=><div className="card" key={r}>{r}</div>)}</div></div></main>; }
function Login(){ return <main className="section"><div className="wrap"><Title e="LOGIN" t="Admin Access" d="Setup mode: open dashboard directly."/><div className="card" style={{maxWidth:520,margin:'auto',textAlign:'center'}}><p className="muted">Demo admin is enabled while Supabase auth is being configured.</p><Link className="btn red" href="/admin">Open Admin Dashboard</Link></div></div></main>; }
function pts(s:string){ if(s==='IN')return 10; if(s==='LATE')return 7; if(s==='OUT')return 5; return 0; }
function Admin(){ const [tab,setTab]=useState('members'); const [rows,setRows]=useState<any>({}); const [loading,setLoading]=useState(false); const tables=['members','attendance','warnings','tryouts','events','alliances','content_plan']; async function load(t=tab){ setLoading(true); try{ const r=await fetch('/api/admin/'+t); const d=await r.json(); setRows((x:any)=>({...x,[t]:Array.isArray(d)?d:[]})); } finally{ setLoading(false); } } useEffect(()=>{tables.forEach(t=>load(t));},[]); async function add(t:string,b:any){ const r=await fetch('/api/admin/'+t,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}); if(r.ok) load(t); } async function patch(t:string,id:string,b:any){ const r=await fetch('/api/admin/'+t+'/'+id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}); if(r.ok) load(t); } async function del(t:string,id:string){ const r=await fetch('/api/admin/'+t+'/'+id,{method:'DELETE'}); if(r.ok) load(t); } const roster=(rows.members||[]).length?rows.members:seedMembers; const tryouts=rows.tryouts||[]; const events=(rows.events||[]).length?rows.events:seedEvents; const allies=(rows.alliances||[]).length?rows.alliances:seedAllies; function MemberForm(){return <form className="card form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);add('members',{real_name:f.get('real_name'),game_name:f.get('game_name'),instagram_id:f.get('instagram_id'),role:f.get('role'),team:f.get('team'),activity_status:'Active',driving_skill:Number(f.get('driving_skill')||0),game_skill:Number(f.get('game_skill')||0),teamwork:80,behavior:80,warnings_count:0,promotion_status:'Trial Running',alliance_tag:'SYN'});e.currentTarget.reset();}}><h3>Add Member</h3>{['real_name','game_name','instagram_id','role','team','driving_skill','game_skill'].map(x=><input key={x} className="input" name={x} placeholder={x.replaceAll('_',' ')} required/>)}<button className="btn red">Add Member</button></form>}
function AttendanceForm(){return <form className="card form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const status=String(f.get('status'));add('attendance',{member_id:f.get('member_id'),date:f.get('date'),status,reason:f.get('reason'),points:pts(status)});e.currentTarget.reset();}}><h3>Mark Attendance</h3><select name="member_id">{roster.map((m:any)=><option key={m.id} value={m.id}>{m.game_name}</option>)}</select><input className="input" name="date" type="date" required/><select name="status"><option>IN</option><option>LATE</option><option>OUT</option><option>NO REPLY</option></select><input className="input" name="reason" placeholder="reason"/><button className="btn red">Save Attendance</button></form>}
function WarningForm(){return <form className="card form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);add('warnings',{member_id:f.get('member_id'),reason:f.get('reason'),warning_level:Number(f.get('warning_level')),action:f.get('action'),date:f.get('date')});e.currentTarget.reset();}}><h3>Add Warning</h3><select name="member_id">{roster.map((m:any)=><option key={m.id} value={m.id}>{m.game_name}</option>)}</select><input className="input" name="reason" placeholder="reason" required/><select name="warning_level"><option>1</option><option>2</option><option>3</option></select><select name="action"><option>Reminder</option><option>Role Hold</option><option>Remove Review</option></select><input className="input" name="date" type="date" required/><button className="btn red">Save Warning</button></form>}
function EventForm(){return <form className="card form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);add('events',Object.fromEntries(f.entries()));e.currentTarget.reset();}}><h3>Add Event</h3>{['title','type','date','time','description','status'].map(x=><input key={x} className="input" name={x} placeholder={x}/>) }<button className="btn red">Add Event</button></form>}
function AllianceForm(){return <form className="card form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);add('alliances',Object.fromEntries(f.entries()));e.currentTarget.reset();}}><h3>Add Alliance</h3>{['clan_name','alliance_tag','leader_instagram','status','notes'].map(x=><input key={x} className="input" name={x} placeholder={x}/>) }<button className="btn red">Add Alliance</button></form>}
return <main className="section"><div className="wrap"><Title e="ADMIN" t="SOUL HQ Dashboard" d="Working dashboard modules with Supabase database storage."/><div className="grid grid3"><div className="card"><div className="stat">{roster.length}</div><p>Members</p></div><div className="card"><div className="stat">{tryouts.length}</div><p>Tryouts</p></div><div className="card"><div className="stat">{events.length}</div><p>Events</p></div></div><div className="tabs">{tables.map(t=><button key={t} className={tab===t?'active':''} onClick={()=>{setTab(t);load(t)}}>{t}</button>)}</div>{loading&&<p className="muted">Loading...</p>}{tab==='members'&&<><MemberForm/><div className="card tablewrap" style={{marginTop:16}}><table className="table"><thead><tr><th>Name</th><th>Role</th><th>Team</th><th>Status</th><th>Promotion</th><th>Action</th></tr></thead><tbody>{roster.map((m:any)=><tr key={m.id}><td>{m.game_name}<br/><span className="muted">{m.real_name}</span></td><td>{m.role}</td><td>{m.team}</td><td>{m.activity_status}</td><td><select value={m.promotion_status||''} onChange={e=>patch('members',m.id,{promotion_status:e.target.value})}><option>Trial Running</option><option>Eligible</option><option>Hold</option><option>Promoted</option><option>Demoted</option><option>Remove Review</option></select></td><td>{String(m.id).length>5&&<button className="small" onClick={()=>del('members',m.id)}>Delete</button>}</td></tr>)}</tbody></table></div></>}{tab==='attendance'&&<><AttendanceForm/><div className="card" style={{marginTop:16}}>{(rows.attendance||[]).map((a:any)=><p key={a.id}>{a.date} • {a.status} • {a.points} points • {a.reason}</p>)}</div></>}{tab==='warnings'&&<><WarningForm/><div className="card" style={{marginTop:16}}>{(rows.warnings||[]).map((w:any)=><p key={w.id}>Level {w.warning_level} • {w.action} • {w.reason}</p>)}</div></>}{tab==='tryouts'&&<div className="grid grid2">{tryouts.map((t:any)=><div className="card" key={t.id}><h3>{t.game_name}</h3><p>{t.name} • {t.instagram_id}</p><p className="muted">{t.message}</p><p>Status: {t.status}</p><button className="small red" onClick={()=>patch('tryouts',t.id,{status:'Accepted'})}>Accept</button><button className="small" onClick={()=>patch('tryouts',t.id,{status:'Rejected'})}>Reject</button></div>)}</div>}{tab==='events'&&<><EventForm/><div className="grid grid3" style={{marginTop:16}}>{events.map((e:any)=><div className="card" key={e.id}><h3>{e.title}</h3><p>{e.type} • {e.time}</p><span className="tag">{e.status}</span></div>)}</div></>}{tab==='alliances'&&<><AllianceForm/><div className="grid grid3" style={{marginTop:16}}>{allies.map((a:any)=><div className="card" key={a.id}><h3>{a.clan_name}</h3><p>{a.alliance_tag} • {a.leader_instagram}</p><span className="tag">{a.status}</span></div>)}</div></>}{tab==='content_plan'&&<div className="card"><h3>Content Planner</h3><p className="muted">Plan reels, stories and clan announcements here.</p></div>}</div></main>; }
export default function Page({params}:any){ const slug=params?.slug?.[0] || 'home'; const map:any={home:<Home/>,about:<About/>,members:<Members/>,join:<Join/>,events:<Events/>,alliance:<Alliance/>,gallery:<Gallery/>,rules:<Rules/>,login:<Login/>,admin:<Admin/>}; return <Shell>{map[slug] || <Home/>}</Shell>; }
